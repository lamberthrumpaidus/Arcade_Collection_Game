const BalapKlik = { sc: 0, tm: 10, init() { GameApp.clr(); this.sc = 0; this.tm = 10; document.getElementById('click-score').innerText = this.sc; document.getElementById('click-time').innerText = this.tm; }, clk() { if (GameApp.isPaused) return; this.sc++; document.getElementById('click-score').innerText = this.sc; if (this.sc === 1) { GameApp.ints.push(setInterval(() => { if (GameApp.isPaused) return; this.tm--; document.getElementById('click-time').innerText = this.tm; if (this.tm <= 0) { GameApp.clr(); alert("Skor Akhir: " + this.sc); this.init(); } }, 1000)); } } };