const TembakTarget = { sc: 0, tm: 30, init() { GameApp.clr(); this.sc = 0; this.tm = 30; document.getElementById('aim-score').innerText = this.sc; document.getElementById('aim-time').innerText = this.tm; this.c = document.getElementById('aim-canvas'); this.x = this.c.getContext('2d'); this.c.onclick = e => { if (GameApp.isPaused || this.tm <= 0) return; let r = this.c.getBoundingClientRect(), s = this.c.width / r.width; let cx = (e.clientX - r.left) * s, cy = (e.clientY - r.top) * s; if (Math.hypot(cx - this.tx, cy - this.ty) < 30) { this.sc++; document.getElementById('aim-score').innerText = this.sc; this.sp(); } }; this.sp(); }, start() { this.init(); GameApp.ints.push(setInterval(() => { if (GameApp.isPaused) return; this.tm--; document.getElementById('aim-time').innerText = this.tm; if (this.tm <= 0) { GameApp.clr(); alert('Waktu Habis! Skor: ' + this.sc); } }, 1000)); }, sp() { this.tx = Math.random() * 340 + 30; this.ty = Math.random() * 340 + 30; this.dr(); }, dr() { this.x.clearRect(0, 0, 400, 400); this.x.font = "50px Arial"; this.x.fillText('🎯', this.tx - 25, this.ty + 15); } };