const IngatPola = { s: [], u: [], init() { GameApp.clr(); document.getElementById('simon-status').innerText = "Tekan Mulai! Skor: 0"; this.s = []; this.u = []; }, start() { this.init(); this.n(); }, n() { this.u = []; this.s.push(Math.floor(Math.random() * 4)); document.getElementById('simon-status').innerText = `Skor: ${this.s.length - 1}`; let i = 0; let t = setInterval(() => { if (GameApp.isPaused) return; if (i >= this.s.length) clearInterval(t); else { let b = document.getElementById(`simon-${this.s[i]}`); b.classList.add('active'); setTimeout(() => b.classList.remove('active'), 400); i++; } }, 800); GameApp.ints.push(t); }, click(i) { if (this.s.length === 0) return; this.u.push(i); let b = document.getElementById(`simon-${i}`); b.classList.add('active'); setTimeout(() => b.classList.remove('active'), 200); if (this.u[this.u.length - 1] !== this.s[this.u.length - 1]) { alert("Salah! Skor: " + (this.s.length - 1)); this.init(); } else if (this.u.length === this.s.length) setTimeout(() => this.n(), 1000); } };