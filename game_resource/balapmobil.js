const BalapMobil = { p: 1, e: [], sc: 0, init() { GameApp.clr(); this.c = document.getElementById('car-canvas'); this.x = this.c.getContext('2d'); this.sc = 0; this.p = 1; this.e = []; document.getElementById('car-score').innerText = this.sc; window.onkeydown = e => { if (e.key.toLowerCase() == 'a' && this.p > 0) this.p--; if (e.key.toLowerCase() == 'd' && this.p < 2) this.p++; }; this.dr(); }, start() { this.init(); GameApp.ints.push(setInterval(() => { if (GameApp.isPaused) return; if (Math.random() < 0.03) this.e.push({ l: Math.floor(Math.random() * 3), y: -50 }); this.e.forEach(v => v.y += 5); this.e = this.e.filter(v => { if (v.y > 400) { this.sc++; document.getElementById('car-score').innerText = this.sc; return false; } return true; }); if (this.e.some(v => v.l === this.p && v.y > 250 && v.y < 350)) { GameApp.clr(); alert('Tabrakan! Skor: ' + this.sc); return; } this.dr(); }, 16)); }, dr() { this.x.clearRect(0, 0, 300, 400); this.x.fillStyle = 'white'; this.x.fillRect(98, 0, 4, 400); this.x.fillRect(198, 0, 4, 400); this.x.font = "40px Arial"; this.x.fillText('🚘', this.p * 100 + 30, 350); this.e.forEach(v => this.x.fillText('🚔', v.l * 100 + 30, v.y + 40)); } };